load("//bazel:api.bzl", "modular_run_binary_test", "mojo_binary", "requirement")

package(default_visibility = ["//visibility:private"])

MOJO_SRCS = glob(["*.mojo"])

[
    mojo_binary(
        name = src.split(".")[0],
        srcs = [src],
        target_compatible_with = ["//:has_gpu"],
        deps = [
            "//max:extensibility",
            "//max:layout",
            "@mojo//:std",
            requirement("numpy"),
        ],
    )
    for src in MOJO_SRCS
]

[
    modular_run_binary_test(
        name = src.split(".")[0] + "_test",
        size = "small",
        binary = src.split(".")[0],
        tags = ["gpu"],
    )
    for src in MOJO_SRCS
]
