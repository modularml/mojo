"""Marker file for things that apply to the entire source tree, excluding external dependencies."""

repo(
    features = [
        "modular_code",
    ],
)
ignore_directories([
    "**/.*.venv",
    "**/.venv",
    "**/.git",
    "**/.pixi",
    "**/node_modules",
])
