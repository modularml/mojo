load("//bazel:api.bzl", "modular_py_library", "requirement")

package(default_visibility = ["//max:consumers"])

modular_py_library(
    name = "nn",
    srcs = glob(["**/*.py"]),
    # See the experimental target: eager examples need the optional
    # _interpreter_ops kernels in the test runfiles.
    docstring_example_deps = ["//max/python/max/_interpreter_ops"],
    # Several examples compile models; shard to stay under the 800s CI clamp.
    docstring_example_shard_count = 4,
    # numpy, engine, and support are used transitively through max.nn,
    # not directly imported in experimental/nn files.
    ignore_extra_deps = [
        requirement("numpy"),
        "//max/python/max/engine",
        "//max/python/max/support",
    ],
    test_docstring_examples = True,
    deps = [
        requirement("numpy"),
        requirement("rich"),
        requirement("typing-extensions"),
        "//max/python/max/driver",
        "//max/python/max/dtype",
        "//max/python/max/engine",
        "//max/python/max/experimental",
        "//max/python/max/graph",
        "//max/python/max/nn",
        "//max/python/max/profiler",
        "//max/python/max/support",
    ],
)
